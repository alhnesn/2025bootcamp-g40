1- objects on plate block picking up the plate (because we removed the PlacedItem layer thingy)
2- when crosshair is off an object, most of the times it doesn't remove highlight.
3- when an object is added to a pan ontop of the stove (or attempted to add to the pan), you can not remove the pan

fix the bugs and TODOs in stove

FIXED:
- When I take a pan with an object on it off the stove and drop it, the physics of it get buggy and it flies. (I think this is because removing a pan from a stove makes it so that the pan does't ignore the collission between the object on it (or the other way around))
- stove is being highlighted (it shouldn't)

FIXED?:
- sometimes can't take back pan after the food on it is cooked.

TO-DO:
- when object placed on CookingPoint, the item on the stove should be highlighted instead of the CookingPoint
- CheeseFull icin adam akilli holding point belirle
- adjust the penalties so the score isn't 0 all the time.
